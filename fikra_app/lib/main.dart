import 'package:flutter/material.dart';

void main() {
  // Entry point of the Flutter application we add commas for better formatting up athour 
  // materailApp widget is the root of the application ---> Center widget centers its child both vertically and horizontally---> Text widget displays a string of text with default styling  
  runApp(MaterialApp(home: Center(child:Text('Hello, World!',),),),);
}